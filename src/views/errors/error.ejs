<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container">
    <div class="row min-vh-100 align-items-center justify-content-center">
      <div class="col-md-6 text-center">
        <div class="mb-4">
          <i class="bi bi-bug text-danger" style="font-size: 5rem;"></i>
        </div>
        <h1 class="display-4 fw-bold text-dark">Error</h1>
        <h2 class="mb-3"><%= typeof title !== 'undefined' ? title : 'Ha ocurrido un error' %></h2>
        <p class="text-muted mb-4">
          <%= typeof message !== 'undefined' ? message : 'Algo salió mal. Por favor, intenta de nuevo.' %>
        </p>
        
        <% if (typeof error !== 'undefined' && error.stack) { %>
        <div class="alert alert-secondary text-start mt-4">
          <pre class="mb-0" style="font-size: 0.75rem; white-space: pre-wrap;"><%= error.stack %></pre>
        </div>
        <% } %>
        
        <a href="/" class="btn btn-primary">
          <i class="bi bi-house me-2"></i>
          Volver al Inicio
        </a>
      </div>
    </div>
  </div>
</body>
</html>
